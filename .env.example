# 服务配置
# 服务监听的端口号
PORT=3000

# 服务监听的主机地址 (0.0.0.0 表示监听所有网络接口)
HOST=0.0.0.0

# Worker 配置
# Worker 进程数量 (推荐 2-8 个，根据实际负载和 CPU 核心数调整)
# 默认使用系统 CPU 核心数，如果核心数较多建议手动设置较小值
WORKERS=4

# 并发控制
# 每个 worker 的最大并发渲染数
MAX_CONCURRENCY=4

# 请求队列最大大小 (当所有 worker 都在忙时，等待队列的最大长度)
MAX_QUEUE_SIZE=16

# 超时配置 (毫秒)
# Keep-Alive 超时时间
KEEP_ALIVE_TIMEOUT_MS=65000

# HTTP Headers 超时时间 (应该比 Keep-Alive 超时时间长)
HEADERS_TIMEOUT_MS=70000

# 请求体大小限制
# 限制请求体的最大大小 (支持的单位: b, kb, mb, gb)
BODY_LIMIT=1mb
